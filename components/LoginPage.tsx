import React from 'react';

const LoginPage = () => {
  return (
    <div style={{ padding: '2rem' }}>
      <h2>請先登入才能進入後台</h2>
      <p>你可以點右上角登入後再返回後台～</p>
    </div>
  );
};

export default LoginPage;
